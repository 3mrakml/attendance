@using Attendence_System.ViewModel
@model LectureWithStudentsViewModel

@{
    ViewData["Title"] = "حضور المحاضرة - " + Model.LectureTitle;
}

<div class="space-y-6">
    <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-4 bg-white p-4 sm:p-6 rounded-2xl shadow-sm border border-slate-100">
        <div>
            <h4 class="text-lg md:text-xl font-bold text-slate-500 mb-1">الطلاب المسجلين في المحاضرة</h4>
            <h3 class="text-2xl md:text-3xl font-extrabold text-slate-800">@Model.LectureTitle</h3>
        </div>
        
        <div class="flex flex-col sm:flex-row w-full md:w-auto gap-3 mt-2 md:mt-0">
            <a href="@Url.Action("Scan", "Lecture", new { id = Hashids.Encode(Model.lectureid) })" class="btn btn-primary w-full sm:w-auto">
                <i data-lucide="scan-line" class="w-4 h-4 mr-2"></i> جهاز المسح
            </a>
            
            <a href="@Url.Action("ExportLectureAttendance", "Lecture", new { id = Hashids.Encode(Model.lectureid) })" class="btn btn-success w-full sm:w-auto">
                <i data-lucide="file-spreadsheet" class="w-4 h-4"></i> تصدير البيانات (Excel)
            </a>
        </div>
    </div>

    <!-- Stats Cards -->
    <div class="grid grid-cols-1 sm:grid-cols-3 gap-4">
        <div class="bg-white p-5 rounded-2xl shadow-sm border border-slate-100 flex items-center gap-4">
            <div class="w-12 h-12 rounded-xl bg-slate-50 text-slate-500 flex items-center justify-center text-xl">
                <i data-lucide="users" class="w-5 h-5"></i>
            </div>
            <div>
                <p class="text-sm font-semibold text-slate-500">إجمالي الطلاب في الصف</p>
                <p class="text-2xl font-black text-slate-800">@Model.TotalStudents</p>
            </div>
        </div>
        <div class="bg-white p-5 rounded-2xl shadow-sm border border-emerald-100 flex items-center gap-4 relative overflow-hidden">
            <div class="w-12 h-12 rounded-xl bg-emerald-50 text-emerald-600 flex items-center justify-center text-xl relative z-10">
                <i data-lucide="check" class="w-5 h-5"></i>
            </div>
            <div class="relative z-10">
                <p class="text-sm font-semibold text-emerald-700">الحضور</p>
                <p class="text-2xl font-black text-emerald-900">@Model.AttendedCount</p>
            </div>
        </div>
        <div class="bg-white p-5 rounded-2xl shadow-sm border border-rose-100 flex items-center gap-4 relative overflow-hidden">
            <div class="w-12 h-12 rounded-xl bg-rose-50 text-rose-600 flex items-center justify-center text-xl relative z-10">
                <i data-lucide="x" class="w-5 h-5"></i>
            </div>
            <div class="relative z-10">
                <p class="text-sm font-semibold text-rose-700">الغياب</p>
                <p class="text-2xl font-black text-rose-900">@Model.AbsentCount</p>
            </div>
        </div>
    </div>

    <!-- Search Bar -->
    <form id="lectureStudentsFilterForm" class="js-auto-filter bg-white p-4 rounded-2xl shadow-sm border border-slate-100"
          data-live-target="#studentsTable">
        <div class="relative">
            <span class="absolute right-4 top-1/2 -translate-y-1/2 text-slate-400 pointer-events-none">
                <i data-lucide="search" class="w-4 h-4"></i>
            </span>
            <input type="text"
                   id="lectureStudentSearch"
                   name="search"
                   class="w-full pr-11 pl-4 py-2.5 rounded-xl border border-slate-200 focus:outline-none focus:ring-2 focus:ring-primary/50 focus:border-primary transition-all text-slate-900"
                   placeholder="ابحث بالاسم أو كود QR..." />
        </div>
    </form>

    <!-- Students Table -->
    <div class="bg-white rounded-2xl shadow-sm border border-slate-100 overflow-hidden">
        <div class="overflow-x-auto">
            <table id="studentsTable" class="min-w-full divide-y divide-slate-100">
                <thead class="bg-slate-50/50">
                    <tr>
                        <th scope="col" class="px-6 py-4 text-right text-sm font-bold text-slate-500 uppercase tracking-wider">الاسم</th>
                        <th scope="col" class="px-6 py-4 text-center text-sm font-bold text-slate-500 uppercase tracking-wider">رمز QR</th>
                        <th scope="col" class="px-6 py-4 text-center text-sm font-bold text-slate-500 uppercase tracking-wider">الحالة</th>
                        <th scope="col" class="px-6 py-4 text-right text-sm font-bold text-slate-500 uppercase tracking-wider">وقت الحضور</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-slate-100">
                    @if (Model.Students != null && Model.Students.Any())
                    {
                        @foreach (var status in Model.Students)
                        {
                            <tr class="hover:bg-slate-50/80 transition-colors group"
                                data-search="@($"{status.Student.FullName} {status.Student.QRToken}")">
                                <td class="px-6 py-4 whitespace-nowrap">
                                    <div class="flex items-center">
                                        <div class="flex-shrink-0 h-10 w-10 rounded-full bg-primary/10 text-primary flex items-center justify-center font-bold">
                                            @status.Student.FullName?.FirstOrDefault()
                                        </div>
                                        <div class="mr-4">
                                            <div class="text-sm font-bold text-slate-900">@status.Student.FullName</div>
                                            <div class="text-xs text-slate-400 font-mono" dir="ltr">@status.Student.QRToken</div>
                                        </div>
                                    </div>
                                </td>
                                <td class="px-6 py-4 text-center">
                                    <span class="font-mono font-bold text-slate-700 text-sm tracking-widest" dir="ltr">@status.Student.QRToken</span>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap text-center">
                                    @if (status.IsAttended)
                                    {
                                        <div class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-bold bg-emerald-100 text-emerald-700 border border-emerald-200 shadow-sm">
                                            <i data-lucide="check-circle" class="w-3.5 h-3.5"></i> حضر
                                            <button type="button" class="text-rose-500 hover:text-rose-700 transition-colors mr-2 cursor-pointer" onclick="removeAttendance('@Hashids.Encode(Model.lectureid)', '@Hashids.Encode(status.Student.StudentId)', '@status.Student.FullName')" title="إلغاء الحضور">
                                                <i data-lucide="trash-2" class="w-3.5 h-3.5"></i>
                                            </button>
                                        </div>
                                    }
                                    else
                                    {
                                        <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-bold bg-rose-100 text-rose-700 border border-rose-200 shadow-sm">
                                            <i data-lucide="x-circle" class="w-3.5 h-3.5"></i> غائب
                                        </span>
                                    }
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap text-sm text-slate-500 font-medium">
                                    @if (status.IsAttended && status.AttendedAt.HasValue)
                                    {
                                        <span dir="ltr" class="inline-block">@status.AttendedAt.Value.ToString("hh:mm tt")</span>
                                    }
                                    else
                                    {
                                        <span class="text-slate-300">-</span>
                                    }
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="4" class="px-6 py-16 text-center">
                                <div class="w-16 h-16 bg-slate-50 text-slate-300 rounded-full flex items-center justify-center mx-auto mb-3 text-2xl">
                                    <i class="bi bi-people"></i>
                                </div>
                                <h4 class="text-lg font-bold text-slate-700">لا يوجد طلاب</h4>
                                <p class="text-slate-500 mt-1">لا يوجد طلاب مسجلين في الصف الخاص بهذه المحاضرة.</p>
                            </td>
                        </tr>
                    }
                    <!-- Empty search row -->
                    <tr class="js-empty-row" style="display:none">
                        <td colspan="4" class="px-6 py-12 text-center">
                            <i data-lucide="search-x" class="w-10 h-10 text-slate-300 mx-auto mb-2"></i>
                            <p class="font-semibold text-slate-500">لا توجد نتائج مطابقة لبحثك.</p>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
        
        <!-- Pagination -->
        <partial name="_Pagination" model="ViewBag.PaginationInfo" />
    </div>
</div>

@section Scripts {
    <script>
        // ── Remove attendance ─────────────────────────────────────────────────
        function removeAttendance(lectureId, studentId, studentName) {
            Swal.fire({
                title: 'هل أنت متأكد؟',
                text: `هل تريد بالفعل إلغاء حضور الطالب (${studentName}) من هذه المحاضرة؟`,
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#ef4444',
                cancelButtonColor: '#64748b',
                confirmButtonText: 'نعم، إلغاء الحضور',
                cancelButtonText: 'تراجع'
            }).then((result) => {
                if (result.isConfirmed) {
                    $.ajax({
                        url: '@Url.Action("RemoveAttendance", "Lecture")',
                        type: 'POST',
                        data: {
                            lectureId: lectureId,
                            studentId: studentId
                        },
                        success: function (res) {
                            if (res.success) {
                                Swal.fire({
                                    icon: 'success',
                                    title: 'تم بنجاح',
                                    text: res.message,
                                    timer: 1500,
                                    showConfirmButton: false
                                }).then(() => {
                                    location.reload();
                                });
                            } else {
                                Swal.fire('خطأ!', res.message, 'error');
                            }
                        },
                        error: function () {
                            Swal.fire('خطأ!', 'حدث خطأ في الاتصال بالخادم.', 'error');
                        }
                    });
                }
            });
        }
    </script>
}
